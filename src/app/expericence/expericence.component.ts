import { Component } from '@angular/core';
import { FormBuilder, FormControl, Validators } from '@angular/forms';
@Component({
  selector: 'app-expericence',
  templateUrl: './expericence.component.html',
  styleUrls: ['./expericence.component.css']
})
export class ExpericenceComponent {
  // ctcForm: any;
  // constructor(private fb: FormBuilder){
  //   this.ctcForm=this.fb.group({
  //     annualCTC:['',[Validators.required, Validators.pattern('^[0-9]')]]
  //   })
  // }

}


